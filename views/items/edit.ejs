<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ADD</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>Update Item description</h1>

    <form action="/items/"<%= user._id %><%= item._id%>?_method="put" method="POST">
            <label for="name">Name of Item</label>
            <input 
            type="text"
            name="name"
            id="foods"
            value="<%= user.foods %>">
            <label for="quantity">Quantity</label>
            <input type="number" id="quantity" name="quantity" min="1" max="100" value="<%=user.quantity%>">
            <label for="description">Description</label>
            <input 
            type="text"
            name="description"
            id="description"
            value="<%= user.description %>">
            <button type="submit">edit item</button>
</form>

</body>
</html>