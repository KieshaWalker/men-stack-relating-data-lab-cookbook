<!DOCTYPE html>
<html>
<head>
    <title><%= otherUser.username %>'s Pantry</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1><%= otherUser.username %>'s Pantry</h1>
    <ul>
        <% if (otherUser.foods.length === 0) { %>
            <li>No items yet.</li>
        <% } %>
        <% otherUser.foods.forEach(f => { %>
            <li>
                <strong><%= f.name %></strong> — Qty: <%= f.quantity %>
                <% if (f.description) { %> - <%= f.description %> <% } %>
            </li>
        <% }) %>
    </ul>
    <p><a href="/users">Back to Community</a></p>
</body>
</html>
